# Logs
# Keep log files out of version control.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Dependency directories
# Exclude installed node modules to avoid bloating the repo.
node_modules/

# Build output
# Ignore distribution bundles generated by Rollup (or other bundlers).
dist/
build/

# TypeScript cache
# tsbuildinfo is generated by tsc when using incremental builds.
*.tsbuildinfo

# Editor metadata
# Ignore temporary and workspace files created by various editors/IDEs.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Operating system files
# Ignore macOS, Windows, and Linux system files.
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Environment files
# Exclude environment configuration files (e.g. containing secrets).
.env
.env.local
.env.*.local

# Jest coverage
# Ignore test coverage directories/files.
coverage/
# If using ts-jest or babel-jest, ignore transform cache.
jest-cache/

# Rollup cache (if using rollup caching)
.rpt2_cache/
# Babel cache
.babel-cache/

# npm and Yarn lockfiles (temporary)
# If you want to track them, you can remove these lines. Usually however,
# lockfiles are committed to ensure reproducible installs—adjust as needed.
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# React Native packager
# Metro cache and temporary files.
.expo/
.expo-shared/
# Only if your RN project uses these directories:
emoji.expo/

# Miscellaneous
# Ignore any files starting with a dot (hidden) except the ones you explicitly track.
.*.sw?